  <div class="fields">
    <div class="min_user">
      <!-- x icon to remove -->
      <%= f.link_to_remove("x") %>
      <!-- fields -->
     <%= f.input :username %>
  	 <%= f.input :password%>
   	 <%= f.input :password_confirmation %>
   	<%= f.label :Role %><br />
   	<%= f.select :role, User::ROLES, :prompt => "Select ..." %>

   	 <%= f.input :active %>
    </div>
  </div>